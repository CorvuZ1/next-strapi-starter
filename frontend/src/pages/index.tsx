import { NextPage } from "next";

const HomePage: NextPage = () => {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gradient-to-r from-cyan-500 to-blue-500 py-5 text-center text-6xl font-bold text-white">
      LET'S GO
    </div>
  );
};

export default HomePage;
